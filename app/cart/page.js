import PrebookingCart from "../../components/PrebookingCart";

export const metadata = {
  title: "Cart & Checkout — Zoha Asif",
  description: "Review and reserve physical first-print editions from Zoha Asif.",
};

export default function CartPage() {
  return <PrebookingCart />;
}
